apiVersion: 1
service:
  id: hiyocord-nexus-web
  name: Hiyocord Nexus Web
  description: Admin dashboard for Hiyocord Nexus (manifest approval, service overview), served via Cloudflare Pages.
  kind: pages
image: node:24
install: npm ci
build: npm run build
artifact: dist
env:
  build:
    - NODE_AUTH_TOKEN
  runtime: []
